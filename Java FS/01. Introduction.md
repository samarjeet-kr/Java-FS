### Spring Syllabus
1. IoC container
2. What is dependency
3. DI -> Dependency Injection
4. Types of DI -> Setter, Constructor, Field injection
5. Autowiring
6. Mapping using -> XML, Annotations & Java file.

### Core Java
1. Anonymous object?
```java
class A{
	A(){}
}
class B extends A{
	B(){}
}
class C{
	B obj = new B();
	B obj; // reference
}

class A extends Object {
	A(){}
}

new B(); // anon object

```

2. Exception can't handled by JVM? Unchecked.
3. Multithreading/Constructor Chaining/Exception Handling.

---
### Spring container
Java Class -> beans -> Object
1. init()
2. service()
3. destroy()

Classes are linked not extended.

![[Excalidraw/FS.excalidraw.md#^area=mkeUVQK4]]

A -> B -> C 
What are the packages of those classes.

Container Types:
1. Bean factory (Basic)
2. Application context (Advance)

- XML vulnerable for Cross Site Scripting so don't use Bean Factory.

Code coupling : Simple class extends class. Fix Dependency Injection, Interface.

```java
class A{
	int a;
	String b;
}
class B{

}

// Constructor
A(int a, String b){
	this.a = a;
	this.b = b;
}

// Method
void setValues(int a, int b){
	this.a = a;
	this.b = b;
}

// Field Injection : Can be any of these two.
```


### Setter Injection
https://mvnrepository.com/artifact/org.springframework/spring-context/7.0.3
https://docs.spring.io/spring-framework/docs/4.2.x/spring-framework-reference/html/xsd-configuration.html

- *bean* - class identity, class name

Steps:
1. Define beans - Car, Engine
2. Configuration -XML
3. Define the container

- Use Internal not catalog

Spring core two - 40.2.2



```xml
<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:util="http://www.springframework.org/schema/util"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           https://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context
                           https://www.springframework.org/schema/context/spring-context.xsd
                           http://www.springframework.org/schema/util
                           https://www.springframework.org/schema/util/spring-util.xsd">


</beans>
```

https://www.springframework.org/schema/beans/


@Configuration

// Marks a class as spring config class

### Autowiring


### ORM - Object Relational Mapping

Technology, Design pattern

![[Excalidraw/FS.excalidraw.md#^area=lu74Em6G]]

Implementation
- Hibernate - ORM tool


![[Excalidraw/FS.excalidraw.md#^area=87oQkJL4]]

1. Configurations
2. Driver class URL
3. JDBC API
4. Username
5. Password

https://mvnrepository.com/artifact/org.hibernate/hibernate-core/5.6.15.Final
https://mvnrepository.com/artifact/mysql/mysql-connector-java/8.0.33

MySQL Config
Custom install
select everything except docs

Password : `123456`

Alternative
Xampp 8.2.12
port 3307
start 2nd
config 2nd
find 3306
change 3307 to all
total 5
save close
close control panel
open again
allow
yes
shell

```
mysql -u root
create database emp_db
use emp_db;
select * from emp;


```

![[Excalidraw/FS.excalidraw.md#^area=etuqoRuw]]

*Hibernet dilect exception :* search the exception edit hubernate-cfg.xml
below password line of session factory


```xml
<!DOCTYPE hibernate-configuration SYSTEM "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Database connection settings -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/your_database_name</property>
        <property name="hibernate.connection.username">your_username</property>
        <property name="hibernate.connection.password">your_password</property>

        <!-- MySQL Dialect property -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>
        
        <!-- Other properties (e.g., show_sql, hbm2ddl.auto) can go here -->
        <property name="hibernate.show_sql">true</property>
        <!-- Mapping files -->
        <!-- <mapping resource="com/yourcompany/YourEntity.hbm.xml"/> -->
    </session-factory>
</hibernate-configuration>

```


Hibernate
DB Storage engine (MyISAM)
- Foreign key ❌
- Join ❌
- Relationship ❌
- Only Table locking ✔️

JPA (ORM)
DB Storage engine (InnoDB)
- Foreign  ✔️
- Join ✔️
- Relationship ✔️
- Table locking, Row locking ✔️


![[Excalidraw/FS.excalidraw.md#^area=gPNzn4Q6]]




`@Configuration`configures beans
`@Bean` used to define beans because we cannot access them without their object.

![[Excalidraw/FS.excalidraw.md#^area=tnAJIZ0W]]

### JDBC SpringBoot No ORM

```
mysql> use empdb
Database changed
mysql> create table employee
    -> (
    -> id bigint,
    -> name varchar(100),
    -> address varchar(100)
    -> );
Query OK, 0 rows affected (0.02 sec)

mysql> desc employee
    -> ^C
mysql> desc employee;
+---------+--------------+------+-----+---------+-------+
| Field   | Type         | Null | Key | Default | Extra |
+---------+--------------+------+-----+---------+-------+
| id      | bigint       | YES  |     | NULL    |       |
| name    | varchar(100) | YES  |     | NULL    |       |
| address | varchar(100) | YES  |     | NULL    |       |
+---------+--------------+------+-----+---------+-------+
3 rows in set (0.02 sec)

```

### JDBC

JDBC Template
- This class has all methods for crud operations.

1. Create
2. Update
3. Delete

update()

read-single - queryForObject()
read-all - query()


### JPA
JpaRepository -> Interface


1. Create Operation:
 - save(E entity)
- saveAll(Iterable<E> entities)

2.    Update Operation :

 save(E entity)

3.    Delete Operation :

 delete(E entity)

 deleteA11(Iterable<? extends E> entities)

 deleteAll( )

 deleteById(Id id)

4.    Read Operation:

 findById(Id id)

 findAll()

 findAllById(Iterable<ID> ids)

 count()

 existsById(Id id)




### Rest Api

